Modify run_drives.sh to include whatever you want to have always running

# install

to run as an app:

```
python3 -m venv venv
. venv/bin/activate
pip install -r requirements.txt
python setup.py py2app
```

At this point you will have an app you can mv into your Applications
folder
